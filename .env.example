# Connexion PostgreSQL (extension pg_trgm requise, créée par scripts/seed.ts)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/sparepart

# URL publique du site (sitemap, canonical, JSON-LD)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
